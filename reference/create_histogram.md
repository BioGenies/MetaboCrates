# Histogram of individual metabolite values before and after imputation

Histogram of individual metabolite values before and after imputation

## Usage

``` r
create_histogram(uncomp_metabo_vals, comp_metabo_vals, metabolite, bins, type)
```
