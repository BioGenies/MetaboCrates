# Density of individual metabolite values before and after imputation

Density of individual metabolite values before and after imputation

## Usage

``` r
create_density(uncomp_metabo_vals, comp_metabo_vals, metabolite)
```
