# Validator on raw_data class

Validator on the content of an raw_data object.

## Usage

``` r
validate_raw_data(raw_data)
```

## Arguments

- raw_data:

  [`raw_data`](http://biogenies.info/MetaboCrates/reference/raw_data.md)
  object.
